# Day08 (8/1 계획 → 8/5 완료, Week B D1) 인출 기록

## 0. 아침 인출 워밍업 — 복습큐 도래분 (8/3·8/4 지연분)

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| W1 | `@Repository`를 지우면 무엇이 없어지나 | "스프링이 관리하는 Bean에서 등록 해제됨" | ⭕ | — (+14로 복귀) |
| W2 | 싱글톤 Service에서 요청별 값을 필드에 두면 안 되는 이유 | "싱글톤은 요청마다 새로운 Bean이 생성되는데 값이 달라질 수 있음" | ❌ | 싱글톤은 **하나만 생성되어 모든 요청이 공유**한다. 요청마다 새로 생기면 오히려 안전하다. 공유되기 때문에 race condition이 생긴다 |
| W3 | `save()`의 저장 계약 | 문항 형식에 대한 지적(코드 없이 답하기 어려움) → 코드 제시 후 재출제 | ❌ | 아래 W3' 참조 |
| W3' | `save(a)` 두 번 호출 후 `store.size()` | "2" | ❌ | **1**. 첫 호출에서 `assignId`가 객체를 변경하므로 두 번째 호출은 `id != null` 분기 → `store.set()` 교체 |
| W4 | Repository가 `Optional`을 반환하고 Service가 하는 일 | "Repository는 DB의 interface 역할만 하므로 Service에게 예외처리를 맡긴다" | ⭕ | Repository는 "없다"는 사실만 보고하고, 그것이 404인지의 도메인 의미 부여는 Service의 책임 |
| W5 | 예외 로그와 500 응답 본문의 차이 | "모르겠음" | ❌ | 원인은 로그로(스택트레이스 전부), 응답은 일반 문구만. `ex.getMessage()` 노출은 SQL·경로·버전 유출 |

**결과: 5문항 중 1.5.** W2·W3·W5 → +1일(8/6) 오답 재시험.

## 1. Day08 인출 (노트·코드 덮고)

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| 1 | Flyway가 하는 일 두 가지 | "SQL 히스토리를 저장하고 마이그레이션을 실행한다" | ⭕ | — |
| 2 | 적용된 `V1`의 컬럼 길이를 늘리려면 | "그 파일을 고치면 체크섬 때문에 실패하므로 새로 작성해야 한다" | ⭕ | `V2__widen_room_name.sql`에 `ALTER TABLE`. `V1`은 불변 |
| 3 | `requesterName`이 컬럼이 될 때 이름과 이유 | "Hibernate 때문에 자동으로 소문자로 변환되므로 snake_case가 좋다" | ⚠️ 부분 | 이유가 **둘**이다. ① Hibernate 네이밍 전략이 camelCase→snake_case로 **단어를 분리**(단순 소문자화 아님) ② DB가 따옴표 없는 식별자를 대문자로 접음. 또한 권장이 아니라 필수 — 컬럼을 camelCase로 만들면 D3에서 Hibernate가 못 찾는다 |
| 4 | `NOT NULL` 컬럼에 `''`가 들어가는가 | "길이만 0일 뿐 NULL이 아니기 때문(들어간다)" | ⭕ | — |
| 5 | `final String roomName`이 DB 값을 못 바꾸게 막는가 | "막아준다. 불변성을 정의하는 것이니" | ❌ | 막지 못한다. `final`은 컴파일러가 JVM 메모리 안 변수 재대입만 검사한다. DB 행은 JVM에 없다 |
| 5' | 앱이 꺼진 상태에서 H2 콘솔의 `UPDATE`는? (A)컴파일에러 (B)런타임예외 (C)그냥 바뀐다 | "A" | ❌ | **C**. 실제 실행으로 확인 — `A101` → `B202`로 변경됨. 자바 코드는 컴파일도 실행도 되지 않았다 |

**결과: 5문항 중 3 정답, 1 부분, 1 오답(반복).**

## 2. 재시험 대상

- **8/6(+1일)** — W2 싱글톤 공유, W3 저장 계약, W5 로그/응답 분리, **Q5 `final`의 작용 범위(오늘 2회 오답)**
- Q3 네이밍은 부분 정답 → 정상 간격(+2)으로 진행하되 D3에서 실물 확인
