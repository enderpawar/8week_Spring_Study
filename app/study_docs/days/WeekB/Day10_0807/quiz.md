# Day10 (8/7 계획 → 8/9 완료, Week B D3) 인출 기록

## 1. 세션에서 보존된 예측

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| P1 | `final`인 두 필드를 둔 채 빈 기본 생성자를 추가하면 컴파일되는가 | "된다" | ❌ | 빈 생성자도 모든 `final` 필드를 초기화해야 한다. 완성 코드에서는 두 필드의 `final`을 제거하고 `protected Reservation() {}`를 유지했다 |
| P2 | 기존 예약을 조회한 뒤 `cancel()`하고 `save()`하면 어떤 SQL이 필요한가 | `SELECT` 1개 + `UPDATE` 1개 | ✅ | 통합 테스트에서 기존 ID 저장 시 `UPDATE ... WHERE id=?`가 실행되고 행이 늘지 않는 것을 확인했다 |

> P1·P2는 유실 전 `progress.md`에 남아 있던 실제 학습자 예측만 복구했다. 그 밖의 답변은 만들지 않았다.

## 2. 완료 확인 질문

아래 문항은 다음 복습 때 노트를 덮고 답한다. 아직 학습자 답을 받지 않았으므로 비워둔다.

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| 1 | JPA, Hibernate, Spring Data JPA를 각각 한 문장으로 구분하면? | | | |
| 2 | `@Id`를 필드에 붙인 것이 애노테이션 접근 위치에 어떤 의미를 주는가? | | | |
| 3 | JPA 기본 생성자를 `public` 대신 `protected`로 둔 이유는? | | | |
| 4 | `JpaRepository`를 상속한 인터페이스의 본문이 비었는데도 실행되는 이유는? | | | |
| 5 | `ddl-auto: none`을 선택한 이유와 Flyway의 역할은? | | | |
| 6 | 기존 예약을 저장했을 때 중복 행이 생기지 않았음을 테스트가 어떻게 증명했는가? | | | |

## 3. 복습 일정

Day10 완료일 8/9를 기준으로 +2일 8/11, +7일 8/16, +14일 8/23에 다시 인출한다. 기존 밀린 복습은 날짜를 옮기지 않고 D7 버퍼에서 처리한다.
