# Day12 (8/22, Week B D5) 인출 기록

## 1. 세션에서 보존된 예측

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| P1 | 관리 중인 Entity를 `cancel()`만 하고 `save()`는 호출하지 않으면, 트랜잭션이 끝날 때 DB에 반영되는가 | "커밋될 것 같아요, dirty checking 때문에" | ✅ | 로그에서 `save()` 호출 없이 `UPDATE`가 실행됨을 확인 |

## 2. 완료 확인 질문

아래 문항은 다음 복습 때 노트를 덮고 답한다.

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| 1 | 변경 감지(dirty checking)가 비교하는 두 값은 각각 무엇인가? | | | |
| 2 | `flush()`는 정확히 언제, 무엇을 트리거하는가? | | | |
| 3 | `save()`를 안 불러도 DB에 반영된다면, `save()`는 왜 여전히 필요한가? | | | |
| 4 | 오늘 실험에서 처음 두 번의 코드가 "아무것도 증명 못 하는 테스트"였던 이유를 설명하면? | | | |

## 3. 복습 일정

Day12 완료일 8/22를 기준으로 +2일 8/24, +7일 8/29, +14일 9/5에 다시 인출한다.
