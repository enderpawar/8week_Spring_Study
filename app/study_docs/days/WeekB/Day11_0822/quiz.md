# Day11 (8/22, Week B D4) 인출 기록

## 1. 세션에서 보존된 예측

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| P1 | `save()` 직후, `clear()` 없이 같은 id로 2회 `findById()`하면 SELECT는 몇 번인가 | "SELECT 한 번, `first == second`는 true" | 절반만 맞음 | 동일성(`true`)은 맞았으나 실제 SELECT는 0번(save() 시점부터 이미 캐시에 있었음) |
| P2 | `clear()` 추가 후 같은 실험을 반복하면 SELECT는 몇 번인가 | "SELECT 1번, 두 번째는 추가 SQL 없음, first==second는 true" | ✅ | 로그와 정확히 일치 |

## 2. 완료 확인 질문

아래 문항은 다음 복습 때 노트를 덮고 답한다.

| # | 질문 | 학습자 답 | 판정 | 교정 |
|---|---|---|---|---|
| 1 | 영속성 컨텍스트와 1차 캐시의 관계를 한 문장으로 설명하면? | | | |
| 2 | 같은 id를 두 번 조회했을 때 `first == second`가 `true`인 이유는 "값이 같아서"가 아니라 무엇 때문인가? | | | |
| 3 | `entityManager.clear()`가 정확히 무엇을 비우는가? | | | |
| 4 | `save()`가 반환한 객체가 이미 영속성 컨텍스트에 올라가 있다는 걸 어떻게 실험으로 증명했는가? | | | |

## 3. 복습 일정

Day11 완료일 8/22를 기준으로 +2일 8/24, +7일 8/29, +14일 9/5에 다시 인출한다.
