# 복습큐 — 간격반복 대기열

형식: 개념 | 최초 학습일 | 다음 도래일 | 상태(대기/완료/오답재시험)

> **「상태」는 마지막 판정 이벤트, 「다음 도래일」은 거기서 계산된 앞으로의 날짜다.** 둘은 서로 다른 것을 가리키므로 어긋나 보여도 정상이다.
> 예: `7/25 | 8/4 | 완료(+2 통과, +7로 복귀)` = 7/28에 +2를 통과해 +7 간격으로 복귀 → 다음은 8/4.
>
> ## 밀렸을 때 규칙 (도래분 > 하루 처리량)
>
> **도래일을 뒤로 미루지 않는다.** 날짜를 옮기면 "얼마나 밀렸는지"라는 증거가 사라진다. 밀린 채로 두고 **매 세션 상위 N건만** 뽑는다(아침 10분 ≈ **5~7문항**).
>
> 뽑는 순서:
> 1. **오답재시험 전부** — 예외 없이 먼저. 틀린 채로 두면 틀린 답이 굳는다(오류 고착).
> 2. **오답 횟수 많은 것** — 3회 > 2회 > 1회.
> 3. **오늘 배울 내용의 선행 지식** — 오늘 쓰이면 그날 실물로 한 번 더 확인된다.
> 4. 나머지는 **도래일이 오래된 순**.
>
> 한 번에 다 따라잡으려 하지 않는다. 밀린 분은 **각 주 D7 버퍼**에서 몰아 처리한다.

| 개념 | 최초 학습일 | 다음 도래일 | 상태 |
|---|---|---|---|
| HTTP 상태코드 기본값 원리 (200 = 이상신호 없음) | 7/25 | 8/4 | 완료(+2 통과, +7로 복귀) |
| ResponseEntity + HttpStatus 명시적 반환 | 7/25 | 8/4 | 완료(+2 통과, +7로 복귀) |
| 메서드 시그니처 중복 컴파일에러 | 7/25 | 8/8 | 완료(오답 교정 통과, +14로 복귀) |
| record DTO vs Domain class 분리 원칙 | 7/26 | 8/4 | 완료(+2 통과, +7로 복귀) |
| record 접근자 명명 규칙(비-JavaBean) | 7/26 | 8/4 | 완료(+2 통과, +7로 복귀) |
| 컴파일러가 못 잡는 오타(문자열·식별자 일치 문제) | 7/26 | 8/9 | 완료(오답 교정 통과, +14로 복귀) |
| `getFieldErrors()`는 실패한 필드만 담는다(전체 필드 아님) | 7/28 | 8/4 | 완료(+2 지연 통과, +7로 복귀) |
| 클래스 `}` 뒤는 최상위 — 메서드가 올 수 없는 이유 | 7/28 | 8/4 | 완료(+2 지연 통과, +7로 복귀) |
| `@RestControllerAdvice` 전역 처리 메커니즘(Bean 등록 + 리졸버 특별 취급) | 7/28 | 8/4 | 완료(+2 지연 통과, +7로 복귀) |
| DTO 정식 명칭 = Data Transfer Object (Data Type object 아님) | 7/28 | 8/4 | 완료(+2 지연 통과, +7로 복귀) |
| `Long`(Wrapper) `==` 비교는 참조 비교 — 값 비교하려면 `.equals()` (작은 값에서 캐싱 때문에 우연히 맞는 것처럼 보일 수 있음) | 7/31 | 8/4 | 완료(+1 재시험 교정 후 통과, +2) |
| Service/Repository 책임 분리 원칙(SRP) — Controller엔 HTTP만, 로직·저장은 각각 위임 | 7/31 | 8/4 | 완료(+1 재시험 통과, +2) |
| 의존성 역전 원칙(DIP) — Service는 구현체가 아니라 인터페이스에 의존 | 7/31 | 8/4 | 완료(+1 재시험 통과, +2) |
| 식별자(PK) 필요성 — id 없이는 "찾아서 갱신" 불가, "새로 추가"만 가능 | 7/31 | 8/4 | 완료(+1 재시험 교정 후 통과, +2) |
| `@PathVariable`-URL 템플릿 불일치는 컴파일이 아니라 런타임(요청 시점)에 터진다 | 7/31 | 8/4 | 완료(+1 재시험 통과, +2) |
| IoC(조립 제어권) vs DI(의존 객체 전달) 구분 | 8/1 | 8/4 | 완료(+1 재시험 통과, +2) |
| `@Repository` 제거는 인터페이스 구현이 아니라 Bean 등록을 없앤다 | 8/1 | 8/19 | 완료(8/5 인출 통과, +14로 복귀) |
| 생성자 이름·실제 필드·의존 타입 일치 | 8/1 | 8/4 | 완료(+1 재시험 교정 후 통과, +2) |
| Singleton Bean 두 번 조회는 같은 참조(`assertSame`) | 8/1 | 8/4 | 완료(+1 재시험 교정 후 통과, +2) |
| Singleton Service는 요청별 변경 값을 지역변수로 둔다 | 8/1 | 8/24 | 완료(8/22 재시험 통과 — "B가 A의 값을 덮어써서 섞인다"까지 설명, +2로 복귀) |
| 생성자 DI + 인터페이스 의존은 구현 변경의 연쇄 전파를 줄인다 | 8/1 | 8/3 | 대기(+2) |
| 저장 계약 — ID가 없으면 신규 추가, 기존 ID면 같은 위치를 교체 | 8/2 | 8/24 | 완료(8/22 재시험 — 처음엔 다시 2로 답했으나 "assignId가 객체 자체를 바꾼다"는 유도 질문 후 스스로 1로 교정, +2로 복귀) |
| `Optional`로 없음 표현 후 Service에서 도메인 예외로 변환 | 8/2 | 8/12 | 완료(8/5 인출 통과, +7) |
| 예상 밖 예외의 상세 메시지는 로그에 남기고 500 응답에는 일반 메시지만 반환 | 8/2 | 8/24 | 완료(8/22 재시험 — 힌트(클라이언트 노출 위험) 후 통과, +2로 복귀) |
| `@Positive` + Spring MVC 메서드 검증으로 경로 변수를 요청 경계에서 검증 | 8/2 | 8/4 | 대기(+2) |
| 단위 테스트와 MockMvc HTTP 테스트의 검증 범위 차이 | 8/2 | 8/4 | 대기(+2) |
| Flyway = 버전 SQL을 순서대로 한 번씩만 실행 + 이력·체크섬을 장부에 기록 | 8/5 | 8/7 | 대기(+2) |
| 적용된 마이그레이션은 불변 — 고치지 말고 `V2`를 쌓는다(체크섬 불일치 시 기동 거부) | 8/5 | 8/7 | 대기(+2) |
| 실패한 마이그레이션은 `success=FALSE`로 장부에 남아 다음 기동을 막는다 | 8/5 | 8/7 | 대기(+2) |
| `NOT NULL`은 `NULL`만 막는다 — `''`는 길이 0인 **값**이라 통과한다 | 8/5 | 8/7 | 대기(+2) |
| 앱 검증(`@NotBlank`)과 DB 제약(`NOT NULL`)은 중복이 아니라 층이 다르다(UX vs 무결성) | 8/5 | 8/7 | 대기(+2) |
| 따옴표 없는 SQL 식별자는 대문자로 접힌다 — 그래서 camelCase가 살아남지 못한다 | 8/5 | 8/7 | 대기(+2) |
| Hibernate 기본 네이밍 전략은 camelCase를 snake_case로 **단어 분리**한다(단순 소문자화 아님) | 8/5 | 8/7 | 대기(+2, 8/5 부분정답) |
| `AUTO_INCREMENT`는 실패·롤백된 번호를 되돌리지 않는다 — 연속이 아니라 유일성만 보장 | 8/5 | 8/7 | 대기(+2) |
| `final`은 JVM 메모리 안 변수 재대입만 막는다 — DB 행과 무관 | 7/31 | 8/29 | 완료(8/22 재시험 — 힌트 없이 즉답 통과, +7로 복귀) |
| `ResultSet` 커서 — `next()`는 행을 옮기고 `getXxx()`는 현재 행을 읽는다 | 8/6 | 8/8 | 대기(+2) |
| 설정 우선순위 — OS 환경변수가 `application.yml`을 이긴다 | 8/6 | 8/8 | 대기(+2) |
| 구현체가 2개면 스프링은 임의로 고르지 않고 기동에 실패한다(fail fast) | 8/6 | 8/8 | 대기(+2) |
| JPA·Hibernate·Spring Data JPA = 명세·구현체·Repository 편의층 | 8/9 | 8/11 | 대기(+2) |
| Entity 기본 생성자와 `final` 필드 초기화 규칙 — 컴파일 자체가 안 됨(Hibernate 런타임 문제 아님) | 8/9 | 8/24 | 오답재시험(8/22 — "불변이라 못 다룬다"는 방향은 맞았으나, 정확한 메커니즘(컴파일러가 초기화 안 된 생성자 경로를 거부)은 힌트 없이 답 못함. 재확인 필요) |
| `@Id` 위치가 필드 접근 방식을 정한다 | 8/9 | 8/11 | 대기(+2) |
| `@GeneratedValue(IDENTITY)`는 INSERT 후 DB 생성 ID를 받는다 | 8/9 | 8/11 | 대기(+2) |
| Spring Data JPA 인터페이스는 런타임 구현체가 생성된다 | 8/9 | 8/11 | 대기(+2) |
| `ddl-auto: none`으로 Hibernate DDL을 막고 Flyway를 스키마의 주인으로 둔다 | 8/9 | 8/11 | 대기(+2) |
| 기존 ID의 `save()`는 갱신되고 중복 행이 생기지 않아야 한다 | 8/9 | 8/11 | 완료(P2 예측·통합 테스트 통과, +2) |
| 1차 캐시 — `save()`/`find`로 한 번 올라온 Entity는 같은 트랜잭션 안에서 재조회해도 SQL 없이 같은 참조를 반환한다 | 8/22 | 8/24 | 대기(+2) |
| `entityManager.clear()`는 영속성 컨텍스트(1차 캐시 포함)를 통째로 비운다 | 8/22 | 8/24 | 대기(+2) |
| 변경 감지(dirty checking) — `flush()` 시점에 로드 스냅샷과 현재 값을 비교해 다르면 자동 `UPDATE`, `save()` 불필요 | 8/22 | 8/24 | 대기(+2) |
| dirty checking은 "지금 값"이 아니라 "로드 시점 값과 지금 값의 차이"를 본다(중간에 왔다갔다 해도 최종값이 같으면 UPDATE 안 나갈 수 있음) | 8/22 | 8/24 | 대기(+2) |
| DTO/Domain 분리 인과관계 — Domain이 상태 변경을 해야 해서 불변(record)로 못 만드는 것이지, "불변이 아니라서 상태변경 로직이 있다"가 아니다 | 8/22 | 8/24 | 대기(+2, D6 누적시험에서 절반만 정답) |
| 생성자 주입(DI)의 이유 2가지 — ①테스트 시 mock 교체 가능 ②구현체 교체 시 Service 코드 무변경(인터페이스 의존) | 8/22 | 8/24 | 대기(+2, D6 누적시험에서 힌트 필요) |
| Flyway 체크섬 불일치와 Hibernate dirty checking은 다른 메커니즘 — 전자는 파일 변조 감지, 후자는 Entity 필드 변경 감지 | 8/22 | 8/24 | 대기(+2, D6 누적시험에서 용어 혼동) |
